Creating a rest API policy using template (JSON)

{
  "mode": "All",
  "policyRule": {
    "if": {
      "not": {
        "field": "type",
        "in": "[parameters('listOfResourceTypesAllowed')]"
      }
    },
    "then": {
      "effect": "deny"
    }
  },
  "parameters": {
    "listOfResourceTypesAllowed": {
      "type": "Array",
      "metadata": {
        "displayName": "Allowed resource types",
        "description": "The list of resource types that can be deployed.",
        "strongType": "resourceTypes"
      },
      "allowedValues": [
        "Microsoft.Compute",
        "Microsoft.Network",
        "Microsoft.Storage"
      ]
    }
  }
}
